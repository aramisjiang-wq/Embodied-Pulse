{
  "name": "embodied-pulse-tests",
  "version": "1.0.0",
  "description": "Test suite for Embodied Pulse",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:api": "jest tests/api",
    "test:unit": "jest tests/unit",
    "test:e2e": "playwright test",
    "test:performance": "node tests/performance/load-test.js",
    "test:all": "npm run test:api && npm run test:e2e && npm run test:performance",
    "generate:test-data": "node scripts/generate-test-data.js",
    "clean:test-data": "node scripts/clean-test-data.js"
  },
  "dependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "axios": "^1.6.0",
    "jest": "^29.7.0",
    "playwright": "^1.40.0",
    "supertest": "^6.3.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
